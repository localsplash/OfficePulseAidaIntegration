--- extensions.conf
+++ extensions.conf
@@ -12,8 +12,8 @@
 
 ; Shared Bandwidth ingress. Each DID must select one client context.
 [from-bandwidth]
-exten => +19496501147,1,NoOp(Routing Concierge DID ${EXTEN})
- same => n,Goto(concierge,inbound,1)
+exten => +19496501147,1,NoOp(Delegating managed Concierge DID ${EXTEN})
+ same => n,Goto(managed-concierge,${EXTEN},1)
 
 exten => +17143750300,1,NoOp(Routing Concierge DID ${EXTEN})
  same => n,Goto(concierge,inbound,1)
@@ -31,6 +31,11 @@
 exten => _X!,1,NoOp(Rejecting unknown Bandwidth DID ${EXTEN})
  same => n,Hangup()
 
+; Dedicated exact Realtime lookup context for the managed Concierge DID.
+; Do not add a Realtime switch to from-bandwidth, default, or shared contexts.
+[managed-concierge]
+switch => Realtime
+
 ; Outbound-channel handler for STIR/SHAKEN Identity.
 ; It becomes active when Bandwidth Dial() calls use b(stir-pre^s^1).
 [stir-pre]
