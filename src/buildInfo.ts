/** npm run build replaces this placeholder with immutable source revision metadata. */
export const buildInfo = Object.freeze({
  version: 'unbuilt',
  revision: null as string | null,
  sourceUpdatedAt: null as string | null,
  dirty: null as boolean | null,
});
